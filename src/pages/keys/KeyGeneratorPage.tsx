import { createToolPage } from '../../components/common';
import { KeyGenerator } from '../../components/keys';

export default createToolPage(KeyGenerator, {
  seoKey: 'keyGenerator',
  canonical: 'https://hsmkit.com/keys-dea',
  toolName: 'Cryptographic Key Generator',
  toolCategory: 'SecurityApplication',
});
